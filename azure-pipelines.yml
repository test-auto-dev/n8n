trigger:
  branches:
    include:
      - main

variables:
  resourceGroup: "my-rg"
  containerAppName: "my-test-services"
  environmentName: "my-env"
  location: "eastus"
  storageAccountName: "n8nstorageacct"
  fileShareName: "n8nfilestorage"

pool:
  vmImage: ubuntu-latest

steps:
  - task: AzureCLI@2
    displayName: "🚀 Deploy persistent n8n to Azure Container Apps"
    inputs:
      azureSubscription: "My-Azure-Service-Connection"
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        set -ex

        echo "🔹 Creating resource group..."
        az group create --name $resourceGroup --location $location

        echo "🔹 Creating Container Apps environment..."
        az containerapp env create \
          --name $environmentName \
          --resource-group $resourceGroup \
          --location $location || true

        echo "🔹 Creating storage account..."
        az storage account create \
          --name $storageAccountName \
          --resource-group $resourceGroup \
          --location $location \
          --sku Standard_LRS || true

        echo "🔹 Creating file share..."
        az storage share create \
          --name $fileShareName \
          --account-name $storageAccountName || true

        echo "🔹 Deploying n8n container..."
        az containerapp create \
          --name $containerAppName \
          --resource-group $resourceGroup \
          --environment $environmentName \
          --image n8nio/n8n:latest \
          --target-port 5678 \
          --ingress external \
          --min-replicas 1 \
          --max-replicas 1 \
          --env-vars \
            N8N_BASIC_AUTH_ACTIVE=true \
            N8N_BASIC_AUTH_USER=admin \
            N8N_BASIC_AUTH_PASSWORD=SuperStrongPassword@123 \
            N8N_USER_MANAGEMENT_DISABLED=false \
            N8N_ENCRYPTION_KEY=mySuperSecretKey123 \
            N8N_PROTOCOL=https \
            N8N_PORT=5678 \
            N8N_PATH=/ \
            WEBHOOK_URL="https://my-test-services.${location}.azurecontainerapps.io/" \
            TZ=Asia/Kolkata \
          --query properties.configuration.ingress.fqdn -o tsv || true

        echo "🔹 Fetching public URL..."
        fqdn=$(az containerapp show --name $containerAppName --resource-group $resourceGroup --query properties.configuration.ingress.fqdn -o tsv)
        echo "✅ Your n8n instance is live at: https://$fqdn"

        echo "🔹 Updating n8n container with correct WEBHOOK_URL..."
        az containerapp update \
          --name $containerAppName \
          --resource-group $resourceGroup \
          --set-env-vars WEBHOOK_URL=https://$fqdn \
          --query properties.configuration.ingress.fqdn -o tsv || true

        echo "✅ Deployment complete. Access n8n at: https://$fqdn"
